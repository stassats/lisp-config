#!/bin/sh

arch=`uname -m`

LISP=/home/stas/lisp
DD=${LISP}/impl/ccl

if [ $arch = x86_64 ]
then
    KERNEL=lx86cl64
else
    KERNEL=lx86cl
fi

CCL_DEFAULT_DIRECTORY=${DD} exec ${DD}/${KERNEL} \
    -n --load ${LISP}/configs/ccl.lisp -b -K utf-8 "$@"
